// LiveCamera.jsx
// Composant responsable de la capture vidéo du créateur via Agora SDK
import React from 'react';

const LiveCamera = () => {
  return (
    <div>
      {/* Caméra et contrôles */}
      <p>Live Camera (placeholder)</p>
    </div>
  );
};

export default LiveCamera;
